linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - goconst
    - gofmt
    - goimports
    - misspell
    - unconvert
    - unparam
    - gosimple

linters-settings:
  goconst:
    min-len: 3
    min-occurrences: 3
  misspell:
    locale: US

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - goconst

run:
  timeout: 10m
  go: "1.24"
